TEST: Sending and Receiving test data by client said
=======================================

Version 1.0 Maxim Sokhatsky

Endpoints
--------

* `actions/1/api/:client` — MQTT

Tuples
------


```erlang
-record('Test', {
    text    = [] :: [] | binary(),
    count   = 0 :: integer(),
    status  = [] :: [] | atom() | msg
}).
```

Overview
--------

User emits Test request and gets test data what was generated by server said.

Protocol
--------

### `Test/msg` — Generate Messages

```
1. client sends `{'Test, Text, Count, msg}`
             to `events/1//api/anon/:client/:token` once.
